export const myUserReducer = (action:any)=>{
    let initialState = {
        userData:{}
    }

    switch (action) {
        case "login":
            
            break;
    
        default:
            break;
    }
}